const initialState = {
    user: {
        
    }
}



export default (state = initialState, action) => {
    const {type, payload} = action;

    let state2 = state;
    switch(type) {

        default:
        break;
    }

    return state2
}